import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saibalaji.societymanagement',
  appName: 'Sai Balaji Society',
  webDir: 'dist/proj-demo',
  server: {
    androidScheme: 'https'
  }
};

export default config;
